{
  "name": "sector-charts",
  "version": "1.0.0",
  "description": "S&P 500 Sector Valuations Dashboard - monorepo root",
  "private": true,
  "scripts": {
    "build": "cd backend && npm run build",
    "start": "cd backend && npm run start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
